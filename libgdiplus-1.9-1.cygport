inherit mono

DESCRIPTION="Mono GDI+ graphics library"

CYGCONF_ARGS="--disable-static --with-cairo=system"

src_install() {
	cd ${B}
	cyginstall

	sed -i -e 's!shouldnotlink=no!shouldnotlink=yes!' ${D}/usr/lib/libgdiplus.la
}
